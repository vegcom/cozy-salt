#!/bin/zsh
# Managed by Salt - DO NOT EDIT MANUALLY

if [ -n "${__ETC_ZSH_ZLOGIN_SOURCED}" ]; then
  return 0
fi

# shellcheck disable=SC1009
if [ -d /etc/login.d ]; then
  # shellcheck  disable=SC1072 disable=SC1073
  for i in $(find /etc/login.d/*.sh 2>/dev/null|sort) ; do
    if [ -r "$i" ]; then
      # shellcheck disable=SC1090
      . "$i"
    fi
  done
  unset i
fi

__ETC_ZSH_ZLOGIN_SOURCED=1
export __ETC_ZSH_ZLOGIN_SOURCED
