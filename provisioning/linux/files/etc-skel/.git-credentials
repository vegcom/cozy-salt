# Git credentials placeholder
# DO NOT store credentials here - use one of:
#   1. SSH keys (recommended): ssh-keygen -t ed25519
#   2. Git credential manager: git config --global credential.helper manager
#   3. Environment variable: GIT_ASKPASS or GH_TOKEN
#
# This file will be replaced by Salt pillar data if configured.
