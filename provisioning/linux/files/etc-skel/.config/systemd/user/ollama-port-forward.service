# Simple unit to forward Ollama pod port
# ~/.config/systemd/user/ollama-port-forward.service
[Unit]
Description=Kubectl Port Forward Ollama

[Service]
ExecStart=/usr/bin/kubectl port-forward --context lab -n inference statefulsets/ollama 11434:11434
Restart=always
RestartSec=5
KillMode=process

[Install]
WantedBy=default.target
