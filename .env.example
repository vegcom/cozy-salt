# Environment configuration (EXAMPLE - copy to .env and fill with actual values)
# This file should NOT be committed to git - see .gitignore

# GitHub Personal Access Token for fetching private/rate-limited repos
# Used by Salt states for git.latest deployments (cozy-vim, cozy-pwsh, etc.)
# Also used in tests/scripts for dynamic repo operations
# To create: https://github.com/settings/tokens
PROJECT_ACCESS_TOKEN=your_github_pat_token_here

# Salt Master MAC address for DHCP reservation (UniFi assigns IP via MAC)
# Default: 02:42:ac:11:00:10
SALT_MASTER_MAC=

# DNS Resolver configure for your network environment
# Can be omitted; however, defaults to 10.0.0.1
SALT_MASTER_RESOLVER=
