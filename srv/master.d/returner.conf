# Master job cache via sqlite3 returner
# All minion returns flow through master — no minion-side changes needed.
# DB persists at /srv/data/salt_returns.db (bind-mounted from ./srv/data/)

master_job_cache: sqlite3

sqlite3.database: /srv/data/salt_returns.db
sqlite3.timeout: 5.0
